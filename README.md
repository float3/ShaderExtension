# ShaderExtension

Work in progress. Editor-side helpers for parsing and editing Unity ShaderLab source: a tokenizer, a preprocessor, `ParsedShader` with typed property declarations, and `ShaderEditor` for writing modified copies back out.

Open the folder as a Unity project; the code is in `Assets/ShaderExtension/Editor/`.
